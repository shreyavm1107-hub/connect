import os
import subprocess
import sys

# Auto-install missing packages on Streamlit's server dynamically
try:
    import sklearn
    import pandas
    import numpy
except ImportError:
    subprocess.check_call([sys.executable, "-m", "pip", "install", "scikit-learn", "pandas", "numpy"])
